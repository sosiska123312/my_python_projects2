# Создание функций
# функции могут называться как угодно но нельзя использовать спец символы

# def test(a, b):
    # return a + b

# res = test(12, 124)
# print(res)




def minimal(lele):
    min_number = lele[0] # инициализация минимального значения
    for lol in lele: # цикл
        if lol < min_number: # если текущее значение меньше минимального
            min_number = lol # обновляем минимальное значение
    return min_number
# Создали функцию позволяющую находить минимальное число в списке

 


nums1 = [4,6,2,7,9,3]
min1 = minimal(nums1)

nums2 = [4,6,7,9,1]
min2 = minimal(nums2)

# if min1 < min2:
    # print(min1)
# else:
    # print(min2)
# Находит минимальное значение среди двух минимальных значений из двух списков





fun = lambda lo, le: lo * le
print(fun(5,2))
# просто пример использования лямбда функций, они могут быть использованы в любом месте где можно использовать функцию


