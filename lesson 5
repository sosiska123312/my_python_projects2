# nums = [5, 7, 4, 8, 5, 7, 9]

# nums[0] = 50 Таким образом можно заменить первое значение в списке на 50
# в одном списке может быть несколько списков



# print(nums[-1],nums[1])


# numb = [5,7,9,2]
# numb.append(100) #добавить
# numb.insert(2,1488) #заменить но при этом не удалить то что стояло вторым по идексу там а просто передвинет вперёд
# b = 5,3,6,7 
# numb.extend(b) #добавить
# numb.reverse() #перевернуть
# numb.sort() #от меньшего к большему
# numb.pop(2) #удалить по индексу
# numb.remove(100) #удаляет определённый символ
# numb.clear() удалит весь список


# print(numb.count(2)) #находит количество определённых символом или значений (кол во совпадений)
# print(len(numb)) #длина списка
# print(numb)




# nums = [5,2,7,'50',False]

# for el in nums:
    # el *= 2
    # print(el)



n = int(input("Enter lenght: "))
user = []
i = 0
while i < n:
    poop = "Enter element #" + str(i + 1) + ": "
    user.append(input(poop))
    i+=1
print(user)
